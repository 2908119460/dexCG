# Robot assets

The network consumes the 96-point Allegro imagination tensor produced from the
xArm6-Allegro kinematic model. URDF/FK integration belongs to the later DexArt
environment adapter; the fixed link contract is defined in
`src/dexcg/robots/allegro.py` and `configs/robot/allegro_xarm6.yaml`.

